
Annotation: 注解，与反射联用
    其实就是代码里的特殊标记，这些标记可以在编译, 类加载, 运行时被读取（通过反射读取）, 并执行相应的处理。
    通过使用 Annotation,程序员可以在不改变原有逻辑的情况下, 在源文件中嵌入一些补充信息（一些新功能）。

关键字：
    @interface

注解和类，接口，枚举为并列结构
    Java文件，MyAnnotation和MyAnnotations为自定义注解
